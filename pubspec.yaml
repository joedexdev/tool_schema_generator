name: tool_schema_generator
description: Generate provider-compatible LLM tool schemas and dispatchers from annotated Dart functions.
version: 1.0.0
repository: https://github.com/joedexdev/tool_schema_generator
homepage: https://github.com/joedexdev/tool_schema_generator

environment:
  sdk: ^3.10.0

dependencies:
  analyzer: '>=8.1.1 <15.0.0'
  build: '>=3.0.0 <5.0.0'
  meta: ^1.18.0
  source_gen: ^4.2.3

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^3.5.15
  coverage: ^1.15.0
  test: ^1.31.1

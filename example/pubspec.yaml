name: example
description: Example usage of tool_schema_generator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  tool_schema_generator:
    path: ../

dev_dependencies:
  build_runner: ^2.15.0
